server:
  port: 8088

spring:

  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://localhost:3306/notificationdb
    username: root
    password: Uday@123

  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com

    port: 587

    username: uday.subtle@gmail.com

    password: iajc lpsu sbyb dykf

    properties:
      mail:
        smtp:
          auth: true

          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka